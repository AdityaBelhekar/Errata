# Errata -- pinned third-party dependency set.
#
# Generated by `pip freeze --exclude-editable` from a clean venv on 2026-08-19,
# Python 3.14.3 / Windows. The eight Errata distributions themselves are NOT listed
# here -- they are installed editable from source by `scripts/setup.sh`, because
# pinning a local editable path defeats the point of a lock file.
#
# Regenerate with:
#   ./.venv/Scripts/python.exe -m pip freeze --exclude-editable > requirements-lock.txt
#
# Runtime (declared in the package pyprojects):
#   lark, Pint, PyYAML  -> errata-valuesem     (FR-4.1: none may reach the network)
#   pydantic            -> errata-spec
#   pymupdf             -> errata-bundle, errata-audit (the PDF text layer)
# Build gates (scripts/ci.sh runs these; they are not importable by the library):
#   fontTools           -> web/datum/tools/font-metrics.py (the LAW §5.3 check)
# Dev tooling (not required to run Errata):
#   pytest, ruff, mypy

annotated-types==0.8.0
ast_serialize==0.8.0
colorama==0.4.6
flexcache==0.3
flexparser==0.4
fonttools==4.63.0
iniconfig==2.3.0
lark==1.3.1
librt==0.15.0
mypy==2.3.1
mypy_extensions==1.1.0
packaging==26.3
pathspec==1.1.1
Pint==0.25.3
platformdirs==4.11.3
pluggy==1.6.0
pydantic==2.13.4
pydantic_core==2.46.4
Pygments==2.21.0
pymupdf==1.28.2
pytest==9.1.1
PyYAML==6.0.3
ruff==0.16.3
setuptools==84.0.0
typing-inspection==0.4.4
typing_extensions==4.16.0
wheel==0.48.0
