catalog: errata-audit demonstration catalog
generated_by: audit/tools/build_demo_catalog.py
source_document: abb-s200-2CDC002142D0207.pdf
warning: THE CATALOG IS CONSTRUCTED. The datasheet is ABB's own and hash-registered; the values Errata
  re-derives and the boxes it draws are read from it. The catalog under audit was generated by this tool
  with defects injected on purpose, because no public ABB catalog feed is available. Detection numbers
  from this demo describe a population we created; grounding is empirical.
mutation: by sha256(sku) % 100 -- reproducible from the SKU list alone, no RNG seed
rows:
- sku: S201M-B6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-B10UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-B13UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-B16UC
  kind: defect
  expected: rated current transposed from 16 A -- a real defect, SEV-1, safety class
- sku: S201M-B20UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-B25UC
  kind: defect
  expected: rated current transposed from 25 A -- a real defect, SEV-1, safety class
- sku: S201M-B32UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-B40UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-B50UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-B63UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-B6UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S202M-B10UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-B13UC
  kind: defect
  expected: rated current transposed from 13 A -- a real defect, SEV-1, safety class
- sku: S202M-B16UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-B20UC
  kind: defect
  expected: rated current transposed from 20 A -- a real defect, SEV-1, safety class
- sku: S202M-B25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-B32UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-B40UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S202M-B50UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-B63UC
  kind: defect
  expected: rated current transposed from 63 A -- a real defect, SEV-1, safety class
- sku: S203M-B6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-B10UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-B13UC
  kind: equivalent
  expected: '''Each'' against a document packing unit of 1 -- the same frame in trade vocabulary, NOT
    a defect. Must not flag'
- sku: S203M-B16UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S203M-B20UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-B25UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S203M-B32UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-B40UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-B50UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S203M-B63UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-B6UC
  kind: defect
  expected: rated current transposed from 6 A -- a real defect, SEV-1, safety class
- sku: S204M-B10UC
  kind: equivalent
  expected: '''Each'' against a document packing unit of 1 -- the same frame in trade vocabulary, NOT
    a defect. Must not flag'
- sku: S204M-B13UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S204M-B16UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S204M-B20UC
  kind: defect
  expected: rated current transposed from 20 A -- a real defect, SEV-1, safety class
- sku: S204M-B25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-B32UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-B40UC
  kind: defect
  expected: decimal point dropped from 0.500 kg -- a real defect
- sku: S204M-B50UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S204M-B63UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-C0.5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-C1UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-C1.6UC
  kind: defect
  expected: decimal point dropped from 0.125 kg -- a real defect
- sku: S201M-C2UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-C3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-C4UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S201M-C6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-C8UC
  kind: defect
  expected: rated current transposed from 8 A -- a real defect, SEV-1, safety class
- sku: S201M-C10UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S201M-C13UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-C16UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-C20UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S201M-C25UC
  kind: defect
  expected: decimal point dropped from 0.125 kg -- a real defect
- sku: S201M-C32UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S201M-C40UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-C50UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S201M-C63UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-C0.5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-C1UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-C1.6UC
  kind: defect
  expected: rated current transposed from 1.6 A -- a real defect, SEV-1, safety class
- sku: S202M-C2UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-C3UC
  kind: defect
  expected: rated current transposed from 3 A -- a real defect, SEV-1, safety class
- sku: S202M-C4UC
  kind: defect
  expected: rated current transposed from 4 A -- a real defect, SEV-1, safety class
- sku: S202M-C6UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S202M-C8UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-C10UC
  kind: defect
  expected: 'packaging frame error: the document states a pack of 5 and the catalog says Each -- the highest-severity
    class in the taxonomy'
- sku: S202M-C13UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-C16UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-C20UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S202M-C25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-C32UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-C40UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-C50UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S202M-C63UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S203M-C0.5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-C1UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-C1.6UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S203M-C2UC
  kind: defect
  expected: decimal point dropped from 0.375 kg -- a real defect
- sku: S203M-C3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-C4UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-C6UC
  kind: defect
  expected: rated current transposed from 6 A -- a real defect, SEV-1, safety class
- sku: S203M-C8UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-C10UC
  kind: defect
  expected: decimal point dropped from 0.375 kg -- a real defect
- sku: S203M-C13UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S203M-C16UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-C20UC
  kind: equivalent
  expected: '''Each'' against a document packing unit of 1 -- the same frame in trade vocabulary, NOT
    a defect. Must not flag'
- sku: S203M-C25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-C32UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-C40UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-C50UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-C63UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K0.2UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K0.3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K0.5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K0.75UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S201M-K1UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K1.6UC
  kind: defect
  expected: rated current transposed from 1.6 A -- a real defect, SEV-1, safety class
- sku: S201M-K2UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K4UC
  kind: defect
  expected: 'packaging frame error: the document states a pack of 10 and the catalog says Each -- the
    highest-severity class in the taxonomy'
- sku: S201M-K5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K6UC
  kind: defect
  expected: rated current transposed from 6 A -- a real defect, SEV-1, safety class
- sku: S201M-K8UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K10UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K13UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S201M-K15UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K16UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K20UC
  kind: defect
  expected: rated current transposed from 20 A -- a real defect, SEV-1, safety class
- sku: S201M-K25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K30UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K32UC
  kind: defect
  expected: decimal point dropped from 0.125 kg -- a real defect
- sku: S201M-K35UC
  kind: defect
  expected: decimal point dropped from 0.125 kg -- a real defect
- sku: S201M-K40UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K50UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-K60UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S201M-K63UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K0.2UC
  kind: defect
  expected: rated current transposed from 0.2 A -- a real defect, SEV-1, safety class
- sku: S202M-K0.3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K0.5UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S202M-K0.75UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K1UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K1.6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K2UC
  kind: defect
  expected: rated current transposed from 2 A -- a real defect, SEV-1, safety class
- sku: S202M-K3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K4UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K8UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K10UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K13UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K15UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K16UC
  kind: defect
  expected: rated current transposed from 16 A -- a real defect, SEV-1, safety class
- sku: S202M-K20UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K30UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K32UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K35UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-K40UC
  kind: defect
  expected: decimal point dropped from 0.250 kg -- a real defect
- sku: S202M-K50UC
  kind: defect
  expected: 'packaging frame error: the document states a pack of 5 and the catalog says Each -- the highest-severity
    class in the taxonomy'
- sku: S202M-K60UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S202M-K63UC
  kind: defect
  expected: rated current transposed from 63 A -- a real defect, SEV-1, safety class
- sku: S203M-K0.2UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K0.3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K0.5UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S203M-K0.75UC
  kind: defect
  expected: decimal point dropped from 0.375 kg -- a real defect
- sku: S203M-K1UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K1.6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K2UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S203M-K3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K4UC
  kind: equivalent
  expected: '''Each'' against a document packing unit of 1 -- the same frame in trade vocabulary, NOT
    a defect. Must not flag'
- sku: S203M-K5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K8UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K10UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S203M-K13UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K15UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K16UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K20UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K30UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S203M-K32UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K35UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K40UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K50UC
  kind: defect
  expected: rated current transposed from 50 A -- a real defect, SEV-1, safety class
- sku: S203M-K60UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-K63UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S204M-K0.2UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K0.3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K0.5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K0.75UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K1UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K1.6UC
  kind: defect
  expected: decimal point dropped from 0.500 kg -- a real defect
- sku: S204M-K2UC
  kind: defect
  expected: rated current transposed from 2 A -- a real defect, SEV-1, safety class
- sku: S204M-K3UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S204M-K4UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K8UC
  kind: defect
  expected: rated current transposed from 8 A -- a real defect, SEV-1, safety class
- sku: S204M-K10UC
  kind: defect
  expected: rated current transposed from 10 A -- a real defect, SEV-1, safety class
- sku: S204M-K13UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K15UC
  kind: defect
  expected: rated current transposed from 15 A -- a real defect, SEV-1, safety class
- sku: S204M-K16UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K20UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S204M-K25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K30UC
  kind: defect
  expected: rated current transposed from 30 A -- a real defect, SEV-1, safety class
- sku: S204M-K32UC
  kind: defect
  expected: rated current transposed from 32 A -- a real defect, SEV-1, safety class
- sku: S204M-K35UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K40UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K50UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K60UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-K63UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z0.5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z1UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z1.6UC
  kind: defect
  expected: rated current transposed from 1.6 A -- a real defect, SEV-1, safety class
- sku: S201M-Z2UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z4UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z6UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S201M-Z8UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z10UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S201M-Z15UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z16UC
  kind: defect
  expected: 'packaging frame error: the document states a pack of 10 and the catalog says Each -- the
    highest-severity class in the taxonomy'
- sku: S201M-Z20UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z30UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z32UC
  kind: defect
  expected: 'packaging frame error: the document states a pack of 10 and the catalog says Each -- the
    highest-severity class in the taxonomy'
- sku: S201M-Z35UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z40UC
  kind: defect
  expected: rated current transposed from 40 A -- a real defect, SEV-1, safety class
- sku: S201M-Z50UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z60UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S201M-Z63UC
  kind: defect
  expected: rated current transposed from 63 A -- a real defect, SEV-1, safety class
- sku: S202M-Z0.5UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S202M-Z1UC
  kind: defect
  expected: decimal point dropped from 0.250 kg -- a real defect
- sku: S202M-Z1.6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-Z2UC
  kind: defect
  expected: decimal point dropped from 0.250 kg -- a real defect
- sku: S202M-Z3UC
  kind: defect
  expected: rated current transposed from 3 A -- a real defect, SEV-1, safety class
- sku: S202M-Z4UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-Z5UC
  kind: gap
  expected: the catalog field is blank where the document states a value -- a fill-rate finding, recoverable
    rather than wrong
- sku: S202M-Z6UC
  kind: defect
  expected: 'packaging frame error: the document states a pack of 5 and the catalog says Each -- the highest-severity
    class in the taxonomy'
- sku: S202M-Z8UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-Z10UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S202M-Z15UC
  kind: defect
  expected: rated current transposed from 15 A -- a real defect, SEV-1, safety class
- sku: S202M-Z16UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-Z20UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-Z25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-Z30UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-Z32UC
  kind: defect
  expected: rated current transposed from 32 A -- a real defect, SEV-1, safety class
- sku: S202M-Z35UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S202M-Z40UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-Z50UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-Z60UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S202M-Z63UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z0.5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z1UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z1.6UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S203M-Z2UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z4UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z5UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z8UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z10UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z15UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z16UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z20UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S203M-Z25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z30UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z32UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z35UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z40UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z50UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z60UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S203M-Z63UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-Z0.5UC
  kind: defect
  expected: rated current transposed from 0.5 A -- a real defect, SEV-1, safety class
- sku: S204M-Z1UC
  kind: equivalent
  expected: the same weight in grams -- a unit-frame difference, NOT a defect. Flagging this would be
    the false positive that ends a pilot
- sku: S204M-Z1.6UC
  kind: defect
  expected: decimal point dropped from 0.500 kg -- a real defect
- sku: S204M-Z2UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S204M-Z3UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-Z4UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S204M-Z5UC
  kind: defect
  expected: rated current transposed from 5 A -- a real defect, SEV-1, safety class
- sku: S204M-Z6UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-Z8UC
  kind: defect
  expected: decimal point dropped from 0.500 kg -- a real defect
- sku: S204M-Z10UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S204M-Z15UC
  kind: defect
  expected: rated current transposed from 15 A -- a real defect, SEV-1, safety class
- sku: S204M-Z16UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S204M-Z20UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-Z25UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-Z30UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-Z32UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S204M-Z35UC
  kind: equivalent
  expected: 'pole count written in trade notation -- semantically identical. FR-5.3: must not flag'
- sku: S204M-Z40UC
  kind: defect
  expected: rated current transposed from 40 A -- a real defect, SEV-1, safety class
- sku: S204M-Z50UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-Z60UC
  kind: correct
  expected: matches the document; the audit should stay silent
- sku: S204M-Z63UC
  kind: equivalent
  expected: '''Each'' against a document packing unit of 1 -- the same frame in trade vocabulary, NOT
    a defect. Must not flag'
- sku: S201M-B100UC
  kind: declined_expected
  expected: this type designation does not appear in the datasheet -- the audit must decline with no_source_document,
    not audit it against the nearest row
- sku: S201MUC-K0.2
  kind: declined_expected
  expected: points at the S200 M UC datasheet, whose ordering tables do not resolve into columns -- the
    layout defeats the table pass, and the audit must say so
- sku: S201M-B16UC-NODOC
  kind: declined_expected
  expected: names a datasheet nobody supplied -- no_source_document
